# Local environment for Caliente-API (keep this file secret / do NOT commit)
# NOTE: This file is included in .gitignore by default. Replace placeholder values as needed.

SPRING_PROFILES_ACTIVE=dev

# App port for local dev
SERVER_PORT=8080

# H2 dev database (in-memory)
DEV_DATABASE_URL=jdbc:h2:mem:caliente_dev_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
DEV_DATABASE_USERNAME=sa
DEV_DATABASE_PASSWORD=

# Local Postgres defaults (for docker-compose or local Postgres)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=caliente_prod_db
DB_USER=postgres
DB_PASSWORD=postgres

# Docker Compose sensitive vars (used by docker-compose.yml)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=caliente_prod_db

# Application secrets (replace in real deployments)
JWT_SECRET=local_dev_change_me
OAUTH_CLIENT_SECRET=local_dev_change_me
# Example environment variables for Caliente-API
# Copy this file to `.env` and replace placeholder values with real secrets.

# Spring profile
SPRING_PROFILES_ACTIVE=dev

# Application port
SERVER_PORT=8080

# Dev (H2) - optional: usually not required for prod
DEV_DATABASE_URL=jdbc:h2:mem:caliente_dev_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
DEV_DATABASE_USERNAME=sa
DEV_DATABASE_PASSWORD=

# Production/Postgres settings (replace CHANGE_ME with real values)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=caliente_prod_db
DB_USER=CHANGE_ME
DB_PASSWORD=CHANGE_ME

# Docker / Compose sensitive variables (used by docker-compose.yml)
POSTGRES_USER=CHANGE_ME
POSTGRES_PASSWORD=CHANGE_ME
POSTGRES_DB=caliente_prod_db

# Optional app secrets
JWT_SECRET=CHANGE_ME
OAUTH_CLIENT_SECRET=CHANGE_ME
